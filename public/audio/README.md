# Background music (optional)

Drop a file named `bg-music.mp3` in this folder to add background music.
No file? The game runs perfectly fine in silence — all the sound effects
are synthesized in-browser and don't need this.

Keep it reasonably small (a few MB) so the game still loads quickly, and
make sure you have the rights to use whatever track you pick.
